exports.validateCreate = (req, res, next) => {
    //Ajout de la validation pour la création d'une action
    next();
  };
  
  exports.validateGet = (req, res, next) => {
    // Ajout de la validation pour la récupération d'une action
    next();
  };
  
  exports.validateUpdate = (req, res, next) => {
    // Ajout de la validation pour la mise à jour d'une action
    next();
  };
  
  exports.validateDelete = (req, res, next) => {
    // Ajout de la validation pour la suppression d'une action
    next();
  };
  