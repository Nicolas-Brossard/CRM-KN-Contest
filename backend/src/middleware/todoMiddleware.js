exports.validateCreate = (req, res, next) => {
    //Ajout de la validation pour la création d'un rappel
    next();
  };
  
  exports.validateGet = (req, res, next) => {
    // Ajout de la validation pour la récupération d'un rappel
    next();
  };
  
  exports.validateUpdate = (req, res, next) => {
    // Ajout de la validation pour la mise à jour d'un rappel
    next();
  };
  
  exports.validateDelete = (req, res, next) => {
    // Ajout de la validation pour la suppression d'un rappel
    next();
  };
  